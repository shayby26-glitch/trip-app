{
  "rules": {
    "users": {
      "$uid": {
        "trips": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    },
    "shared": {
      "$tripId": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}
